# Few-shot

- Артефакт Практики 1:
- Что хотим улучшить:
  - practice_02/artefact/tests_load.md — добавить конкретные числовые пороги и воспроизводимые сценарии на основе problem.md и TRAINING_PR.diff.

## Примеры

### Хороший результат
| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Параллельные запросы | 10 rps, 5 мин, 50 воркеров | p95 < 500 ms; 5xx = 0% | p50/p95 latency, доля 5xx | app/api.py:35-38 |
| Превышение размера | 1 запрос с diff 150 KB | 413 Payload Too Large | Код ответа, время до отказа | app/api.py:36-38 |

### Плохой результат
| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Большой diff | Отправить большой diff | Не должен падать | Время | app/api.py |

## Запрос
См. промт техники: [few_shot.md](./few_shot.md)

## Что получили
Обновлённый tests_load.md с 5 сценариями и конкретными числами: добавлены параллельная нагрузка, таймаут LLM, DoS через неожиданные типы. Все строки снабжены evidence вида file:line из TRAINING_PR.diff.

## Что изменили в исходном артефакте

- Файл и раздел:
  - practice_02/few_shot/tests_load.md — таблица сценариев нагрузочного тестирования и раздел «Как использовали AI».
- Изменение:
  - Добавлены 3 новых сценария по технике few-shot, конкретизированы пороги (p95 ≤ 500 ms, 5xx = 0%, лимит 100 KB — TO BE), ссылки на evidence из diff (app/api.py:35-38; app/api.py:36-38; app/review_service.py:19-22).
- Как проверили:
  - Сверили метрики с practices/practice_01/problem.md:18 (p95 ≤ 500 ms), :17 (2xx ≥ 99%), :19 (лимит 100 KB — TO BE). Сопоставили evidence с practices/practice_01/TRAINING_PR.diff: app/api.py:35-38; app/review_service.py:19-22.
- Что отклонили:
  - Любые предположения вне TRAINING_PR.diff (например, конкретные реализационные лимиты таймаутов или внутренние ограничения LLM).

## Diff (было / стало)

Было (practice_02/artefact/tests_load.md):

```markdown
| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Большой diff | Отправить diff ~150 KB | Ожидаемый лимит 100 KB (TO BE) | Должен вернуться 413; без лимита — рост латентности/стоимости | app/api.py:36-38 — отсутствуют проверки размера; app/review_service.py:19-22 — прямая вставка diff |
| Долгий вызов LLM | 1 запрос, LLM-заглушка задерживается на 60 сек | TO BE: таймаут и контролируемая ошибка | Поведение при зависании | app/review_service.py:19-22 (нет таймаута) |
```

Стало (practice_02/few_shot/tests_load.md):

```markdown
| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Большой diff | Отправить diff ~150 KB | Ожидаемый лимит 100 KB (TO BE) | Должен вернуться 413; без лимита — рост латентности/стоимости | app/api.py:36-38 — отсутствуют проверки размера; app/review_service.py:19-22 — прямая вставка diff |
| Долгий вызов LLM | 1 запрос, LLM-заглушка задерживается на 60 сек | TO BE: таймаут и контролируемая ошибка | Поведение при зависании | app/review_service.py:19-22 (нет таймаута) |
| Параллельные запросы | 10 rps, 5 мин, 50 воркеров | p95 ≤ 500 ms; 5xx = 0% | p50/p95 latency, доля 5xx | app/api.py:35-38 — POST без валидации; app/api.py:36-38 — прямой доступ к payload["diff"] |
| Таймаут LLM (граничный) | 1 запрос, LLM-заглушка задерживается на 60 сек | TO BE: таймаут на границе API; контролируемая ошибка (4xx/5xx), доля 5xx < 1% | Время до отказа, тип кода ответа | app/review_service.py:19-22 — вызов LLM без таймаута |
| DoS через неожиданные типы | 10 rps, 1 мин; payload не dict или отсутствует ключ "diff" | TO BE: 422 Unprocessable Entity; 5xx = 0% | Доля 4xx/5xx, устойчивость под нагрузкой | app/api.py:36-38 — прямой доступ к payload["diff"] без проверки |
```
